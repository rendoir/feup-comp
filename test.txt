module program1 {
  data=[100]; // vector of 100 integers
  a[] = 1;
  N = 100;
  c[] = [N];
  N;
  mx;         // attribute mx
  mn;         // attribute mn

   function a[]=det(d[], A[], b) {
    wtf = data + c;
    i = + 2;
    M = d.size - 1; //d.size is the same as d.length (Java)
    while (data < c) { // version not optimized
      a = d[i]; i=i+1; b=d[i];
      mx = library1.max(a, b);
      mn = library1.min(a, b);
    }
   }

  function main() {
    vars = [2];
    varss = det(vars, vars, N);
    varsss = 2;
    det(varss, vars, varsss);
    io.println("max: ", mx);
    io.println("min: ", mn);
  }
}
