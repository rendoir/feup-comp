module program1 {
  data=[100]; // vector of 100 integers
  a[] = 1;
  cenas;
  N = 100;
  c[] = [data]; //Error 1 cant use array as size
  undef[] = [cenas];  //Error 2 cant use undefined (not initializd)
  undef2 = [cenas];   // Error 3 not initialized
  N;
  mx;         // attribute mx
  mn;         // attribute mn

  function main() {
    vars = [2];
    varss = det(vars, vars, N);
    varsss = 2;
    det(varss, vars, varsss);
    io.println("max: ", mx);
    io.println("min: ", mn);
  }

   function a[]=det(d[], A[], b) {
    wtf = data + c;  // Error 4 cant use 2 arrays
    i = + 2;
    d.size = 256;
    if (i == 2) {
      variable1 = 1;
    }
    else {
      variable1 = 2;
    }

    M = d.size - variable1;
    while (data < c) { // Error 5 cant use 2 arrays
      a = d[i]; i=i+1; b=d[i];
      mx = library1.max(a, b);
      mn = library1.min(a, b);
    }
   }
}
