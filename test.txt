module program1 {
  data=[100]; // vector of 100 integers
  a[] = 1;
  N = 100;
  c[] = [N];
  mx;         // attribute mx
  mn;         // attribute mn

   function a[]=det(d[], A[], b) {
    data.size = 100;
    i = + 2;
    M = d.size - 1; //d.size is the same as d.length (Java)
    while (i < M) { // version not optimized
      a = d[i]; i=i+1; b=d[i];
      mx = library1.max(a, b);
      mn = library1.min(a, b);
    }
   }

  function main() {
    vars = [2];
    varss = [1];
    varsss = 2;
    det(vars, varss, varsss);
    io.println("max: ", mx);
    io.println("min: ", mn);
  }
}
